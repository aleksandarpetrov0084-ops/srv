

// Each processor to have his own log_processor and log_worker processor for read/write logs to sqlite db
// Think there has to be a db_processor , db_worker and db that will be spined off the msg_processor


Classes to create:
Templates

wrkr_type
wrkr_type_service


msg_handler is what i need some way to figure out how to route messages   